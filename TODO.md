# To do

Last updated: 2026-09-25

## Infrastructure (shared)

- [x] **New 2026-09-23 (Jonathan): removed repeating overdue reminder emails, added a due-back calendar invite instead** — `sendReturnReminders()` (`.gas-proxy/Code.js`) no longer sends/pushes an "Overdue" nag every 7 days once a loan passes its due date (`sendOverdueReminder_()` deleted); the due-soon reminder ahead of the due date is unchanged. In its place, every new loan now gets an Outlook/Gmail/Apple Calendar `.ics` invite for its due-back date the moment it's created (`index.html`'s `sendReturnCalendarInvite()`, called from `submitBatchLoan()`'s `doLoan()`, hits a new `action:'returnInvite'` branch on the same Simpro proxy endpoint — `sendReturnCalendarInvite_()`). The invite's built-in reminder is island-aware, same rationale as the existing shipping-notice window: **North Island** gets a 3-day-ahead reminder, **South Island** a full week (7 days), to cover the extra inter-island return freight time. Needs testing: confirm a new loan's AM actually receives the `.ics` invite and it opens correctly in Outlook, with the reminder firing at the right offset for each island.

- [x] **Fixed 2026-09-17: DNS/HTTPS fix for demo.chsnz.co.nz landed** — IT enforced HTTPS; confirmed via `curl -I http://demo.chsnz.co.nz/` returning a real `301` to `https://` (previously a `200` with no redirect at all) and `https://demo.chsnz.co.nz/` serving normally. Unblocks everything below that was held on it.
  - [x] **Done 2026-09-17**: `index.html`'s `APP_URL` changed from `http://demo.chsnz.co.nz` to `https://` — every loan confirmation/extend/manage link sent by email now points at the secure URL.
  - ⚠️ **Still needs manual verification (Google Cloud Console, not something I can check)**: confirm the Firebase Web API key (`FIREBASE_CONFIG.apiKey`) doesn't have an HTTP-referrer restriction that excludes `https://demo.chsnz.co.nz/*`.
  - ✅ **Expected, not a bug**: every AM/operator's saved sign-in (`chs_operator`, remembered mobile identity, Test Mode) was scoped to the old `http://` origin — browsers treat http/https as separate origins, so everyone needs to sign in again once on `https://`.
  - ✅ Partial fix already landed earlier: 192×192/512×512 manifest icons, a separate blocker for Android installing as a real standalone app (not a bookmark) — combined with HTTPS now landing, Android install quality should be fully fixed. Needs testing: reinstall the Android home-screen icon and confirm it opens standalone (no address bar), not as a bookmark.
- [x] **Confirmed 2026-09-11**: camera scanning works on Android. iOS camera **access** also confirmed working (not the installed home-screen icon, which has its own separate permanent WebKit bug, bugs.webkit.org #185448 — manual asset-number entry remains the deliberate fallback there).
- [x] **Fixed 2026-09-17: iOS camera opens but doesn't decode QR codes** — was on hold pending the DNS fix, now implemented. Root cause: iOS Safari has never shipped the `BarcodeDetector` API (Chromium-only), so it fell back to `jsQR` alone — a pure-JS decoder noticeably less tolerant of blur/distance/angle in a live video frame than native barcode detection. Added ZXing (`@zxing/library` 0.23.0, loaded via jsDelivr) as a first attempt in `mpOpenScan()`'s no-`BarcodeDetector` fallback branch, decoding straight from the video frame (`BrowserQRCodeReader.decode(video)`, no canvas draw needed); jsQR stays loaded and runs as a second attempt on the same frame (still needed either way for desktop's file-upload scanner, `handleNativeQR()`) and as the sole fallback if the CDN script fails to load. Only touches the no-`BarcodeDetector` path — Android's `BarcodeDetector` branch returns before ever reaching this code, unaffected. **Confirmed working on a real iPhone.**
  - [x] **Follow-up 2026-09-17: noticeably slower than Android** (confirmed working, but reported as slow to lock onto a code) — found the fallback was running jsQR's full canvas-draw-and-decode right after ZXing on every single tick where ZXing simply hadn't found anything YET, which is the overwhelmingly common case (a code sits in frame for many ticks before it locks on). That doubled the per-tick CPU work for no benefit once ZXing is confirmed loaded. jsQR now only runs as the fallback when ZXing's CDN script never loaded at all. Some residual gap vs. Android is inherent (pure-JS decode vs. hardware-accelerated `BarcodeDetector`, no way around that), but this redundant work was a real, avoidable contributor on top of it. Needs testing: re-confirm scan speed on the same iPhone.
- [ ] Delete the leftover stuck-loan test case in Firebase (`TEST New Booking (Claude Test Case)`, blocking asset `100516`) once done testing it — it'll otherwise keep sending a real daily reminder email/push.
- [x] **Fixed 2026-09-11: "Enable push notifications" button showed up on Android over plain HTTP** — `mpPushSupported()` only checked whether `Notification`/`serviceWorker` exist as properties, which Chrome exposes even over HTTP (unlike Safari, which hides `navigator.mediaDevices` entirely in that case). The actual push APIs behind the button genuinely need HTTPS, so tapping it would have failed. Added an explicit `window.isSecureContext` check. Needs testing (re-confirm the button is now gone on Android/HTTP, and still appears correctly once on HTTPS).
- [x] **Unblocked 2026-09-17: push notification testing on iOS** — was blocked because GitHub Pages auto-redirected the raw `cmchsgit.github.io` URL to the custom domain, which was HTTP-only; now that `demo.chsnz.co.nz` itself serves HTTPS, no workaround/temporary domain removal is needed to test on iOS any more. Needs testing: opt in on a real iPhone and confirm a push notification arrives and tapping it opens the app.
- [x] **Confirmed 2026-09-11: push notification pipeline fully works end-to-end** — `testSendPushOnly()` succeeded (FCM accepted the send), and the notification arrived on the Android test device (didn't show as a heads-up banner at first — that's just Android's per-app notification importance setting for Chrome, not a bug, fixed by setting Chrome's notification channel to Urgent/High). Tapping the notification opens `TRACKER_URL` (`https://demo.chsnz.co.nz/`, hardcoded in `.gas-proxy/Code.js`) — this previously failed with a cert error since the domain had no valid HTTPS cert; now that HTTPS is enforced (2026-09-17), this should work end-to-end. Needs re-testing to confirm.
- [x] **New 2026-09-17: in-app "Test Notifications" panel** — on both desktop (Settings → Test Notifications) and mobile (Service & Projects' More → Test notifications). Pick any account manager, optionally start from a scenario preset (Demo reminder / Return due soon / Overdue / Stuck-loan reassign — same wording as the real reminders, or Custom for free text), and send a real push straight to their registered device(s) via a new `action:'testPush'` branch on the existing Simpro proxy (`sendTestPush_()` in `.gas-proxy/Code.js`, reusing `sendPushToPerson_()`) — lets delivery/wording/tap-through be verified on a real Android or iPhone without waiting for a real trigger to fire or faking loan data. The device-picker shows a note when the chosen person has no registered device yet (checked client-side against the already-loaded `accountManagers` list) and disables Send in that case. Needs testing on both platforms.
  - [x] **Changed 2026-09-17: restricted to Service & Projects, password gate removed** — originally gated behind the shared `ADMIN_PASSWORD` (same as Test Mode/Account Managers); replaced with a role check instead, since a shared password isn't really "restricted to a person/role." Desktop hides the whole Settings section unless `currentOperator` resolves to Service & Projects (`updateTestNotifVisibility()`, re-run every time Current User changes); mobile already only showed the More-screen button to that role. The proxy call now sends `requesterName` (`currentOperator`/`_mp.name`) instead of a password, and `sendTestPush_()` independently re-checks that name's role against Firebase server-side (via the same `amEffectiveRole_()` the real reminder triggers use) before sending — so this is enforced server-side, not just hidden client-side.
- [x] **Root-caused 2026-09-17: real Android device was receiving zero pushes at all** (reported as "not getting notifications on Android" despite OS-level Chrome notification permissions all correctly enabled) — confirmed via the Test Notifications panel's new per-token FCM response (see below) that **every one of the account's 4 registered tokens was `UNREGISTERED`**. A push subscription/token is tied to its exact origin; moving `demo.chsnz.co.nz` from `http://` to `https://` permanently killed every token registered beforehand. Fixed in two parts:
  - [x] `sendPushToPerson_()` (`.gas-proxy/Code.js`) now returns a per-token `{ok, detail}` result instead of only `Logger.log`-ing it (this project has no Cloud Logging wired up, so that was otherwise invisible) — `sendTestPush_()` surfaces it in its response, which is how the dead tokens were actually confirmed rather than guessed at.
  - [x] Any token FCM reports `UNREGISTERED`/`NotRegistered` for now gets auto-pruned from that person's `fcmTokens` (`pruneDeadTokens_()`) — previously nothing ever removed a dead token, so it'd be retried forever by every future reminder. Confirmed working live: the affected account's `fcmTokens` went from 4 (all dead) to none.
  - [x] Fixed: re-enabling push after this (tapping "Enable/Re-register" again with zero tokens registered) now delivers successfully — confirmed by the user.
- [ ] **Android push still shows as coming from Chrome, not the app itself — unlike iOS**, and a notification was flagged **"Possible spam"** (reported 2026-09-17, after the above delivery fix). Two separate things:
  - The "via Chrome" attribution is Android's own decision based on whether a **WebAPK** exists for the origin (only created once genuinely "Installed", not bookmarked) — checked at the moment a notification is shown, not baked into the token at registration time (corrects an earlier note here that assumed the latter). No icon or push-payload change can force this.
  - **Checked on the actual device**: Settings → Apps shows no separate app entry at all (only Chrome) — confirms no WebAPK exists. Chrome's own 3-dot menu offers only a single "Install and create shortcut" item, not a distinct full "Install app" — this itself is a symptom that Chrome isn't treating the site as fully installable on this device, though the specific failing criterion hasn't been pinned down yet: manifest (name/icons/start_url/scope/display) and service worker registration all check out on inspection, and HTTPS is confirmed live. Google's remote Lighthouse/PageSpeed API is quota-blocked without an API key, so it couldn't be checked that way.
  - [x] **Found and fixed 2026-09-17**: the user ran Chrome's own manifest/installability check and got back two "Icon ... failed to load" errors — this was the actual installability blocker. `manifest.json`'s three icons (192/512/180px) were embedded as inline base64 `data:` URIs rather than real files; Chrome's WebAPK-minting service fetches manifest icons over the network to bake into the generated APK, and can't fetch a `data:` URI that way, so it silently fell back to offering only a plain shortcut instead of a full install (matching the single "Install and create shortcut" menu item observed, no separate app in Settings → Apps). Fixed by extracting all three to real `icon-192.png`/`icon-512.png`/`icon-180.png` files at the repo root and pointing `manifest.json` at them; `index.html`'s `apple-touch-icon` link (byte-identical to the 180px icon) and `sw.js`'s push-notification icon/badge now reference the same files instead of each separately embedding the same base64. `sw.js`'s `CACHE_NAME` bumped so already-visited devices actually refetch the corrected manifest rather than keep serving the stale broken one. **Needs testing**: reinstall on the Android device now that this is fixed, and re-run the same Chrome manifest check to confirm both icon errors are gone.
    - [ ] **Separate, lower-priority note from the same check**: Chrome also reported no `screenshots` entries in the manifest (one needed with `form_factor: "wide"` for desktop, one without/other than `wide` for mobile) — this only affects the nicer "richer install UI" preview card, not whether install/WebAPK works at all. Not addressed yet.
  - "Possible spam" is very likely a side effect of testing, not a real bug — five generic, content-free test pushes ("Diagnostic test 1-5") were sent to the same device in quick succession while diagnosing the delivery issue above, which is exactly the pattern Chrome's local per-device heuristic flags. Should fade as real notifications are sent normally; tapping "Not spam" on one directly un-flags it. Avoid sending repeated back-to-back test pushes to the same person going forward for this reason.
  - [x] **Fixed in passing**: `sw.js`'s offline HTML fallback (`caches.match`) did an exact-URL match including query string, but only the bare `/` is ever pre-cached — so an offline load of `/?mobile=1` (the actual `start_url`) or any deep link on a device that never fetched that literal query variant online first would fail outright despite the identical page being cached. Now matches with `ignoreSearch: true`.

## Mobile

### Pending work

- [x] **Fixed 2026-09-17: `openStuckLoanReassign()` now opens the mobile Transfer screen, not desktop's** — reported live: tapping a stuck-loan alert on mobile (Today tab, or an upcoming loan's "Reassign / transfer" action) opened the desktop reassign modal instead of `mpOpenReassignFlow()`, showing up as a desktop-styled popup over the mobile UI (the modal is a top-level element outside `#app`, not covered by mobile's usual UI hiding). Now branches on `isMobilePWA()` — mobile gets the native Transfer screen, pre-ticked to just the blocking item(s) with the stuck booking pre-picked as the move-to target, same as desktop's own pre-fill. Needs testing: tap a stuck-loan alert on mobile and confirm the native Transfer screen opens correctly pre-filled, not the desktop popup.
- [x] **Push notifications** — confirmed fully working end-to-end 2026-09-11 (see Infrastructure section above) on Android. Firebase console setup (VAPID key, service-account key) is done. The DNS/HTTPS fix landed 2026-09-17, so iOS testing (install-to-home-screen, then opt in) is now unblocked — not yet actually tested there.
- [ ] **App icon badge only updates while the app is open** — no push-driven update yet, so it won't tick up while the app is closed/backgrounded. Would need the push payload to carry the AM's current count and `sw.js`'s push handler to call `self.registration.setAppBadge()`.
- [x] **Demo-loan reminder push** — `sendDemoReminders()` now also pushes to every Service & Projects team member's device, alongside the existing email (Jonathan, 2026-09-11). Needs testing (see below) — still needs the same Firebase console setup as the stuck-loan push before it can actually deliver.
- [ ] **Alerts screen is still just one toggle** — no in-app history list or per-notification-type toggles, per the original mockup.
- [ ] **Kit-builder not on mobile** — built into desktop's Database tab only (see Desktop section). Flag if mobile parity is wanted.
- [x] **Fixed 2026-09-17: desktop's print-doc editor could pop up on mobile after Add items** — reported live (an AM finishing an Add items action saw the old desktop loan form appear). Root cause: `_doBasketAddToLoan()` (shared by desktop's Database-tab tick-to-add and mobile's Add items screen) always finished with a desktop confirmation dialog whose "Done" button opens `#loan-doc-modal` — a top-level element outside `#app`, not covered by mobile's usual UI hiding — to merge the new items into the saved loan doc and persist it. `mpBookConfirm()` already had a `mp-suppress-loan-doc` trick for this exact problem on a brand-new loan, but Add items never used it. Extracted the merge-and-save logic into `finishAddItemsDoc()`, now run headless (suppressed, no dialog, just a toast) when `isMobilePWA()` is true; desktop's dialog and editor are unchanged. Needs testing: add items to a loan from mobile and confirm no desktop-styled popup ever appears, just a success toast, and that the loan doc's item list actually reflects the addition afterward (check via desktop's "View loan form").
- [x] **New 2026-09-17: sticky search + selected list on mobile Add items** — same `position:sticky` technique Jonathan's equipment-tab fix used. The loan name/search header now stays fixed while scrolling "Free for these dates," and the "Selected (N)" list moved into that same sticky block (was previously the top of the scrolling list) so what's already picked stays visible while scrolling for more — capped at 33vh with its own internal scroll so a large selection can't swallow the whole screen. Needs testing.
- [x] **Follow-up 2026-09-17: collapse the selected list past 2 items** — reported live with 7 selected devices: even capped at 33vh, a sticky "Selected" block eats permanent screen space, and a growing pick list started crowding out the actual search area. Added a shared `mpSelectedSummaryHtml()` — under the threshold still shows the full list as before, at or past it collapses to a one-line "Selected (N)" + Expand toggle. Applied to both Add items and the Equipment tab's pinned-to-top selected section when picking devices for a new loan (same underlying complaint, just not sticky there — it still dominated the top of the list before you reached anything new to browse). Threshold (`MP_SELECTED_LIST_AUTOCOLLAPSE`) was originally 5, lowered to 2 same day per feedback that 5 still let it grow noticeably before collapsing. Needs testing.
- [x] **Follow-up 2026-09-17: Equipment tab's Selected section moved into the sticky header too** — it collapsed correctly (above) but stayed pinned to the top of the *scrolling* list rather than the sticky search/Select header, so scrolling still moved it out of view, unlike Add items. Moved into a new `#mp-eq-selected` inside the same sticky block as the search bar, matching Add items exactly. Needs testing: pick 2+ devices for a new loan and confirm "Selected" stays visible with the search bar while scrolling.

### Needs testing

- [ ] All six mobile-native loan actions: Add items, Return items, Extend, Reassign (scheduled-edit form, active-loan move, and "Create new loan with these items"), Delete loan, Loan form.
- [ ] Scan tab's Check out / Return / Count modes, especially the "on loan when counted" two-button branch.
- [ ] AM and Service & Projects tab bars now match (both: Today/Overview, Loans, **Book** center, Equipment, More) — Service & Projects' Scan moved to a header icon top-right instead of a tab. Confirm this reshuffle looks/works right for both roles.
- [ ] Equipment multi-select → "Add N devices to loan" hand-off into the Book wizard, the Book draft surviving a tab switch, and selected devices pinning to the top of the list regardless of search/filter.
- [ ] New item detail screen (mobile-native rebuild) — Check out/Check in actions, recent activity list, and the anti-double-tap "✓ Counted/Returned/Checked in" confirmation state added 2026-09-11.
- [ ] Action-required badge — tab icon and home-screen app icon, on both Android Chrome and iOS 16.4+ if available.
- [ ] The scheduled-loan dedup fix — a real (not test-case) split loan should show as one card with a pending sub-list, not two cards.
- [ ] Audit log source badges — do something from mobile, confirm it shows 📱 Mobile.
- [ ] "Manage team roles" correctly hidden from a real AM's More screen, still visible when a service person previews the AM view.
- [ ] **New (Jonathan, 2026-09-11): mandatory Shipping Island (North/South)** field on loan creation, both desktop and mobile — drives island-aware reminder lead time (North: 3 business days notice, South: 5). Confirm the field is required, saves correctly, and the reminder timing math looks right for both islands.
- [ ] **New (Jonathan, 2026-09-11): same-day loans allowed** — end date can now equal start date instead of being rejected. Confirm this works on both the desktop batch form and the mobile Book wizard.
- [ ] **Test/sign-off tracking moved from Active/Overdue to Scheduled-only loans** (2026-09-11, per feedback) — the Awaiting-test/Tested pill and Mark as tested/No test required actions no longer show on Active/Overdue loans (equipment's already with the customer by then), only on Scheduled ones. Also added the missing "No test required" mobile action (desktop's "Skip" already existed; mobile only had "Mark as tested"). Needs testing.
- [ ] **Date-field overlap on real iPhone 14** — reported again 2026-09-11 on Book-a-demo Step 1 despite the earlier flex-wrap/min-width fix (which looked fine elsewhere but not on that real device — iOS Safari's native `<input type="date">` has its own minimum content width that can exceed the CSS threshold used to decide when to wrap). Fixed by stacking Start/End date fields vertically instead of attempting side-by-side, in both Book-a-demo Step 1 and Reassign's scheduled-loan edit form. Follow-up fix same day: the stacked date fields still rendered visibly *taller* than every other field on a real iPhone (`.mp-input`'s `min-height` doesn't constrain iOS's native date control) — fixed with an explicit `height:48px` override for `input[type=date/time].mp-input`.
  - [ ] **Reported again 2026-09-17 on the scheduled-loan Edit form**: height was right this time, but the date text/calendar icon sat "stretched and not centred" inside the 48px box. Root cause: the `height:48px` fix normalized the outer box but never set a matching `line-height`, so WebKit's internal day/month/year segments still centered per their own default line-height inside the taller box — plus those segments carry their own default padding that pushes them further off-center and can stretch unevenly to fill leftover width. Added `line-height:48px` and zeroed the `::-webkit-datetime-edit-*` segment padding.
  - [ ] **Reported AGAIN 2026-09-17 (still v145, confirmed not a stale cache) on Book-a-demo Step 1**: still visibly "stretched" — the `line-height` fix didn't hold either. Confirmed iOS Safari's native date/time control simply ignores an author-specified height/line-height on the input itself (documented WebKit behaviour, not a specificity/selector issue on our end). Changed approach entirely instead of a 4th attempt at the same technique: each date input is now wrapped in a plain `.mp-date-wrap` div that owns the border/background/height, with `overflow:hidden` clipping anything the native control renders past it — ordinary CSS on an ordinary div, which holds regardless of what the input does internally. Applied to all 5 mobile date inputs (Book-a-demo Step 1 ×2, Extend, scheduled-loan Edit form ×2). **Needs re-confirming on a real iPhone yet again** — this bug has now had four fix attempts; if this one still isn't right, it likely needs an actual screen recording or in-person look rather than another blind CSS guess.
- [x] **Fixed (Jonathan, 2026-09-11): sticky "Add devices to loan" footer** — was unreachable on a long equipment list (sticky styles were on a wrapper sized to fit only the button row, not a child of the taller screen container). Verified with a real scroll.
- [x] **Changed (Jonathan, 2026-09-11): Scan auto-starts the camera** — no longer auto-focuses the manual asset-number field (which used to pop the keyboard and cover "Start camera"); camera starts automatically on load and after switching Check out/Return/Count modes, with the button as a fallback if the stream fails.
- [x] **Improved (Jonathan, 2026-09-11): clearer camera error message over HTTP** — when the camera fails specifically because the page loaded over `http://` (the DNS/HTTPS issue above), the toast now says so directly and suggests opening the `https://` URL, instead of just the raw secureContext/mediaDevices/UA diagnostic string.
- [x] **Fixed 2026-09-11: duplicate/unclickable entries in Today's "Coming up"** — same unmerged groups+bookings bug already fixed in the Loans tab's Upcoming segment, just never applied to `mpUpcomingForName()` (used by Today's "Coming up" list and Upcoming stat count). A booking split across a scheduled group and a leftover upcomingLoans entry (the "equipment being held" case) showed up twice with nothing to tap into. Rewritten to reuse the already-correct `mpAllUpcoming()`; rows now navigate via `mpJumpToLoan()`. **Needs testing** — was reported live via Jess's AM view (Garwei Ho / Conference Magic duplicated).
- [x] **Fixed 2026-09-11: Awaiting-test/Tested pill hidden from AM view** — was showing on the AM's own scheduled-loan cards even though test/sign-off is a Service & Projects responsibility (the kebab actions were already service-only, the pill itself wasn't). Needs testing.
- [x] **Fixed (Jonathan, 2026-09-11): date formatting + equipment sort order** — several spots (history table/modal, database card view, mobile Add-items/equipment-detail on-loan lines, item detail Manufacture/Purchase Date, Excel export) showed either a raw unformatted ISO date or a locale-dependent format that could silently vary by device region — now always dd/mm/yyyy via explicit 'en-NZ' locale. Mobile's equipment list (Equipment tab + Add-items screen) now sorts by asset number ascending like desktop, instead of arbitrary data order. Needs testing.
- [x] **Fixed 2026-09-11: Overview's "Upcoming — needs testing" showed already-tested loans** — reused the same unfiltered upcoming-loans list the stat tile count uses, so a loan (e.g. NZ Anaesthetic Techs Society Conference, confirmed already Tested on both desktop and mobile) never dropped off the list once marked tested. Added a separate filter for the section specifically (excludes tested/no-test-required and isUpcoming bookings with no equipment yet), leaving the stat tile's total count untouched. Needs testing.
- [x] **Fixed (Jonathan, 2026-09-11): the "✓ Counted/Returned" confirmation never actually showed** — root cause of what looked like my anti-double-tap fix not working: `submitCount()`/`submitCountKeepLoan()`/`submitReturn()` all call `clearScan()` (nulls the shared `currentItem`) and then still read `currentItem.CHSAssetNo` afterward for their success toast, throwing an uncaught error on every single call (both platforms) that silently aborted whatever ran next — including mobile's re-render that was supposed to show the ✓ state and the freshly-updated Counted date. The write always worked; only the follow-up UI update never ran. Fixed by capturing the asset number before `clearScan()` runs. Needs testing.
- [x] **New (Jonathan, 2026-09-11): Stocktake mode** — 4th Scan segment, admin-password-gated (writes in bulk with no per-item confirmation). Once unlocked, each scan (camera or manual entry) immediately marks the item counted & returned and keeps the camera running for the next one — no per-item "Mark as counted" tap or camera restart. Shows a brief ✓ + running tally + recent-scans list; 2.5s cooldown per decoded value to avoid re-processing the same code sitting in front of the camera. Needs testing.
- [x] **Changed (Jonathan, 2026-09-15): Scan queues multiple items with a Confirm step** — Check out/Return/Count no longer act on each scan immediately; scanning now queues items into a running list per mode (same pattern Stocktake's continuous scan already used), so a mis-scan can be removed from the list before committing. Confirm is the one point that actually writes — Check out hands the queued assets to the Book wizard, Return/Count apply as one batch with a single summary toast. Needs testing.
- [x] **Overhauled (Jonathan, 2026-09-11): overdue + due-soon reminders moved server-side** — the old overdue check only ran once per page load client-side (so it silently did nothing most weeks unless someone had a tab open at the right moment). Replaced with a real daily Apps Script trigger that emails **and pushes** the responsible AM (repeating every 7 days while still overdue), plus a brand-new "due back soon" reminder (email + push to the AM, push to Service & Projects) using the same island-aware notice window as the outbound shipping reminder — so South Island return loans now get a full business week's notice too. Applies to every loan type, not just Demo. **Needs testing** — verified server-side with a sandboxed harness, but not yet against real data/devices.

## Desktop

### Pending work

- [ ] **Structural: loan identity collides when two loans share the same customer/location/end date** — `_loanKey()` (used almost everywhere to look up "which loan") is built purely from customer name + location + end date, with no BatchID in it. Two genuinely different loans (different BatchIDs) with identical details — e.g. an accidental duplicate booking — generate the *same* key, so actions like "Add items" can silently resolve to the wrong one. Root-caused a real incident this way (Conference Magic - QT Queenstown Hotel, 2026-09-15): an end-user's accidental duplicate booking, once returned and re-added to the correct loan, kept its own BatchID instead of merging, because the two loans were indistinguishable by key at the moment the action ran. Fixed the immediate data by hand; the structural fix (making loan identity collision-proof) is a bigger change, not yet designed.
- [x] **New 2026-09-15: warn before creating a duplicate loan** — `submitBatchLoan()` (shared by desktop and the mobile Book wizard) now checks whether an active loan or upcoming booking already exists for the exact same customer/location/end date, and warns (soft confirm, not a hard block) before creating a second one — the same mistake that caused the incident above. Needs testing: try booking a new loan matching an existing one's details.
- [x] **New 2026-09-15: vibrate + chime on successful scan (Check out/Return/Count/Stocktake)** — fires as soon as a scanned code matches a real equipment item, in all four Scan modes. Vibration (`navigator.vibrate()`) is **Android only** — Safari has never implemented the Vibration API at all, on any iOS version; no code-side workaround exists (unlike the camera bug). The chime is a synthesised beep (Web Audio, no audio file) added alongside it specifically because it *does* work on iOS, so there's still a success cue on both platforms. Needs testing on both — the chime in particular depends on iOS's user-gesture audio unlock actually working as primed in `mpOpenScan()`.
- [x] **New 2026-09-15: transfer note recorded in Simpro job notes on reassign** — moving items between loans changed each job's item list with no explanation of where they went/came from. Now appends a system note ("Transferred N item(s) (...) to X on date" / "Received N item(s) (...) from Y on date") to each side's Simpro notes on every push, including the source job's final update right before it closes — so a closed job's record still explains what happened, not just an empty item list. Kept separate from the loan's own editable notes field. Needs testing.
- [x] **Fixed 2026-09-15: source Simpro job left open when a reassign transfers out its last item** — the reassign/move-items flow (`confirmReassignAM()`, shared by desktop and mobile) only updated the source loan's Simpro job, and only if items remained on it afterward. Transferring *all* items off a loan skipped that check entirely, leaving the job open in Simpro forever with a stale item list — nothing ever told Simpro the job was done. Now closes it in that case, same as a real return already does when it empties a batch (`confirmBatchReturn()`); `closeSimproJob()` already writes a `simpro_closed` audit entry, so that's covered too. Needs testing: transfer every item off a loan via Reassign and confirm the source job closes and shows up correctly in the Audit Log.
- [x] **Fixed 2026-09-15: missing audit log coverage for several delete/remove actions** — cancelling or removing an item from an upcoming loan, removing an account manager, permanently deleting a retired asset (single or bulk), deleting a kit group or one of its accessories, deleting a test report, and deleting History entries all had no audit trail at all. Added `writeAudit()` calls with new types (`upcoming_loan_deleted`, `upcoming_item_removed`, `am_removed`, `retired_asset_deleted`, `kit_deleted`, `kit_accessory_removed`, `test_report_deleted`, `history_deleted`) and their badge styles/labels. Also fixed the pre-existing `test_report_uploaded` type, which had no label and fell back to showing the raw type string. Needs testing — do each action and confirm it now shows up correctly in the Audit Log.

- [x] **New 2026-09-15: clearing a loan's Job # sends it back to Missing Simpro Jobs** — if a job turns out not to actually exist in Simpro (rather than just being the wrong number), clearing the "Job #" prompt's field (instead of typing a new one) removes the recorded number and the loan reappears in Missing Simpro Jobs, where "Create Job" makes a real fresh one via the Simpro API.
- [x] **New 2026-09-15: click a loan's "Job #" badge to correct a wrong Simpro job number** — "Link" (above) only covers a loan with no job recorded at all; there was no way to fix one that's already recorded but wrong (deleted/archived in Simpro afterward, or crossed with a different loan's number — found via a real case, Ciaran Cliff's loans showing job numbers that don't match Simpro's own pending-jobs list). Click the "Job #" text on any loan card (active, scheduled, or upcoming) to type in the correct number. Neither this nor Link ever calls the Simpro API — both only correct our own record. Needs testing.
- [x] **New 2026-09-15: "Link" an existing Simpro job in Missing Jobs** — "missing" only ever meant `loanDocs/{pdfKey}.simproJobId` wasn't set in Firebase, not that no job actually exists in Simpro; previously the only option was "Create Job," which would make a duplicate if one had already been created directly in Simpro. Added a "Link" action per row — type the existing job number, it's recorded straight away and the loan drops off the missing list. Needs testing.
- [x] **Fixed 2026-09-15: `submitReturn()` never closed the Simpro job when a return emptied a batch** — the most common return path (desktop Scan/item-detail Return, and every mobile return flow) only ever deleted the loanDoc when a batch's last item came back; it never told Simpro the job was done, leaving it open indefinitely with a stale item list. Root-caused via a real example (Mill Road Veterinary Clinic, job #209287 — created 20/07, all items returned 09/09, job still showing "New" in Simpro afterward). Fixed with the same Firebase-fallback job-ID lookup `confirmBatchReturn()` already used correctly. `submitCount()`'s existing (weaker) close-check hardened the same way. A follow-up audit of historical data (auditLog + history + loanDocs cross-reference) found ~29 real prior loans left open in Simpro by this bug; user cleaned those up by hand in Simpro directly. Needs testing: return the last item of a batch on both desktop and mobile, confirm the job closes.
- [x] **New 2026-09-15: closing note recorded in Simpro job notes on return** — `closeSimproJob()` itself only ever flips Stage to Complete, never touching the job's own notes, so a job closed by a return alone went quiet with whatever description it had from creation. Added `_closeSimproJobWithNote()` — fetches the loanDoc, appends "All items returned to office on [date]." (or "All items counted and returned to office on [date]." for Count) to the existing notes, pushes that with an empty item list, then closes — mirroring the transfer note already left on reassign. Wired into all three return-triggered close paths: `submitReturn()`, `submitCount()`, `confirmBatchReturn()`. Needs testing.
- [x] **New 2026-09-17: split a Scheduled loan's "Reassign" into separate Edit and Transfer actions** — the single button (mobile and desktop) actually only ever opened the full edit-all-fields form for a Scheduled loan (`openReassignAMByKey()`/`mpOpenReassignFlow()` branched purely on `g.scheduled`); there was no way to move one or more of its items onto a different loan at all, unlike an Active loan which already had exactly that. Now both are offered as separate actions ("Edit" / "Transfer") via an explicit mode, and Transfer's target-loan list (desktop + mobile) also offers other Scheduled loans, not just Active ones and unconfirmed bookings. An Active loan keeps just the one (renamed "Reassign" → "Transfer", since that's all it ever did). Also fixes `openStuckLoanReassign()`, which always needed transfer mode to move a blocking item away but previously fell into the edit form whenever the blocking loan was Scheduled — a gap its own comment already flagged, with no way to resolve the block from there. Needs testing: on a Scheduled loan, confirm Edit and Transfer both work and Transfer can target another Scheduled loan; confirm the stuck-loan "Tap to reassign" shortcut now works when the blocking loan is Scheduled.
- [x] **Merged 2026-09-17 (Jonathan): renamed desktop loan-card buttons** — "Reassign" → "Edit or Reassign Loan" landed just before the Edit/Transfer split above; reconciled by superseding it with the actual split (Edit + Transfer as separate buttons) rather than a combined label, including the unconfirmed-booking card's equivalent button (which only ever edits, so it's just "Edit" now).
- [x] **New 2026-09-21: renamed "Account Managers" to "Manage your staff", added a search bar** — the hamburger-menu entry point and modal title (plus the matching hint text and code comments) now read "Manage your staff." The "Account Manager" role option inside the role dropdown and the "All Account Managers" loan-filter dropdown elsewhere are untouched — different concepts (a role name, and which person a loan is assigned to), not this admin screen. Also added a search-by-name-or-email input above the list so finding someone in a long staff list doesn't mean scrolling — filters before rendering but keeps each row's original index into `accountManagers[]`, since `updateAMField()`/`removeAccountManager()` both index straight into that array. Needs testing.
- [x] **Fixed 2026-09-21: printable/emailed loan form showed items no longer on the loan** — root-caused via a real loan (NZ Private Hospital Conference): its loan form listed 10 items while the actual device list only had 8. `generateLoanDoc()` prefers a loanDoc's `savedRows` over the live equipment list whenever `savedRows` exists, and nothing ever pruned it when an item left the batch via a **partial** return or count (only the "whole batch now empty" case was handled — closes the Simpro job and deletes the loanDoc entirely). Two of this loan's items had been counted-and-returned days earlier via `submitCount()` while the rest stayed on loan, stranding them in `savedRows`/`items` indefinitely. Added `_pruneLoanDocItems()`, wired into the "batch still has other active items" branch of `submitReturn()`, `submitCount()`, and `confirmBatchReturn()` — mirrors the `savedRows` pruning `confirmReassignAM()` already does correctly for its own transfer-out case. This specific loan's loanDoc was also corrected directly in Firebase, since the code fix doesn't retroactively fix already-stale data. Needs testing: return/count one item out of a multi-item batch (not the last one) and confirm the loan form no longer shows it.
- [x] **Fixed 2026-09-21: Add items silently wiped a loan's Demo type/Shipping island/Job type** — same loan again: its Demo type badge, present right after creation, had disappeared. `saveLoanDocChanges()` does a full PUT of the loanDoc, and its save body never carried `demoType`/`island` forward at all (`jobType` was only half-covered). Fine for the one caller that shows the person this form to edit, but every other caller that triggers a re-save without them looking at it — "Add items" in particular, desktop's own "Done" button and mobile's headless equivalent both routing through the same shared `finishAddItemsDoc()`/`saveLoanDocChanges()` — only ever passes through loanTo/location/dates/accountManager, never those three. This loan had 4 "Add items" actions after creation; any one would have reset them to blank. `saveLoanDocChanges()` now always reads the existing loanDoc first (previously only done when the simproJobId wasn't already cached) and falls back to its `demoType`/`island`/`jobType` when the caller didn't supply one. This loan's actual original Demo type is unrecoverable — nothing recorded what it was before this wiped it, so it was **not** guessed/backfilled; it needs re-selecting once via Edit, then should survive Add items normally from then on. Needs testing: add items to a loan that has a Demo type/Shipping island set and confirm both survive afterward.
- [x] **New 2026-09-21: Edit now available on Active loans, not just Scheduled** — direct follow-up to the fix above: two more real loans (Waitemata configuration meeting, Defib configuration meeting) lost their Demo type the same way, but by the time this was noticed both were already Active — and Active loans only ever had "Transfer," no way to correct a field at all. `confirmReassignAM()`'s edit branch never actually cared about scheduled-vs-active (it PATCHes the loanDoc's contact/notes/shipping/demoType, a merge, not the full-overwrite PUT that caused the wipe bug), so this was purely a UI restriction. `openReassignAMByKey()`/`mpOpenReassignFlow()` now trust an explicit mode fully; both the desktop Active loan card and mobile's Active-loan action sheet offer "Edit" alongside "Transfer" now, same as Scheduled loans. Needs testing: Edit an Active loan (desktop + mobile), confirm Demo type/other fields save correctly and nothing about the loan's dispatched status is disturbed.
- [x] **New 2026-09-21: "Other…" on desktop's Demo type dropdowns, custom values persisted and shared** — while fixing the above, noticed desktop's Demo type selects (`#b-demo-type` New Loan, `#reassign-demo-type` Edit) only ever had the 3 fixed options — no way to enter anything else at all, unlike mobile which already had an "Other" sheet for free text. Added a shared `demoTypeOptionsHtml()`/`onDemoTypeSelectChange()` — picking "Other…" prompts for a value. That value is now also saved to a new `/customDemoTypes.json` array (`addCustomDemoTypeIfNew()`), so it shows up as a real selectable option for **everyone**, on **both** platforms, afterward — not free text that has to be retyped from scratch every time. Mobile's own "Other" sheets (Book wizard + Edit form) now also list previously-saved custom types as regular tappable options. Needs testing: enter a new custom Demo type via desktop's "Other…", confirm it then appears as a normal dropdown option (both desktop and mobile) without needing to type it again.
- [x] **Fixed 2026-09-21: audit log entries could silently disappear (confirmed NOT a one-off — 7 loans across two months)** — started investigating one missing `loan_created` entry (NZ Private Hospital Conference); cross-referenced every loan creation in the device history log (unaffected by this bug) against the audit log and found 7 across two months with no entry at all, a ~16% miss rate, ruling out coincidence. Root cause: `auditLog` lived as a field inside `/data.json` — the same document equipment/history live in — saved via `saveBin()`'s full-overwrite PUT, gated by a check-then-act version guard with a genuine race window: two near-simultaneous saves from different sessions could each pass the conflict check before either write landed, and whichever PUT landed second silently overwrote the other's newer audit entries. `writeAudit()` now writes each entry to its own `/auditLog/{id}.json` key instead, independent of the equipment/history save cycle — two sessions writing different keys can never collide the way two PUTs to the same document can. `saveBin()` no longer touches `auditLog` at all; `loadData()`/`syncFromFirebase()` load/refresh it from its own path, same pattern as `loanMeta`/`upcomingLoans`. Migrated the existing 277 entries (plus one older orphaned entry already sitting at that path from some earlier point in this app's history) into `/auditLog.json` directly via the Firebase REST API before deploying, so nothing already recorded was hidden by the switch — verified with a real write/read/delete round-trip against the new path afterward. The 7 confirmed-missing historical entries were **not** backfilled/fabricated — this fixes the mechanism going forward, not the historical record. Needs testing: do a few loan actions from both desktop and mobile, confirm every one shows up in the Audit Log, including right after another session was actively saving at the same time if that's easy to arrange.
- [x] **Fixed 2026-09-25: transferring items onto an unconfirmed upcoming booking could show the item twice** — reported as a test loan card showing the same asset both directly and again under "Awaiting return before pickup" right after a Transfer (turned out to be a stale browser tab in that specific instance, but the underlying code path had a real, reproducible version of the same bug). `confirmReassignAM()`'s Transfer branch, when the target is an unconfirmed upcoming booking (`targetVal.startsWith('upcoming:')`), sets the moved item's BatchID to match the upcoming loan's own id but never removed it from that upcoming loan's own `items` list — unlike the normal `activateUpcomingLoans()` path, which always shrinks or deletes the entry once its items are matched. The stale entry then kept matching in `renderLoans()`'s `_pendingItems` merge (same `id` === the item's live BatchID now), so the item rendered a second time as "pending" even immediately after the render that follows every transfer. Now mirrors the same cleanup: removes the moved item(s) from the target upcoming loan's `items` array, deleting the whole entry if that empties it out. Needs testing: Transfer an item from an active/scheduled loan onto an unconfirmed upcoming booking and confirm it shows up once, not twice.
- [x] **Fixed 2026-09-25: retired asset's "Edit" button silently did nothing** — reported while trying to fix a typo in a retired asset's notes. The detail panel's Edit button is shared between normal and retired items (`editFromDetail()` → `openEditModal(id)`), but `openEditModal()` only ever looks in the `equipment` array — a retired item lives in `retiredAssets` instead, so it found nothing and returned silently. Added a `_detailIsRetired` flag (set when `openDetail()`/`openRetiredDetail()` opens the panel) so Edit now branches to a small new modal for retired items — Reason, Retired Notes, and Notes only, since serial number/dates/prices etc. shouldn't normally change after retirement. Avoids the previous only option (Restore to Database → edit → re-retire), which also reset the retired date. Needs testing: open a retired asset, Edit, correct a field, confirm it saves and shows correctly in the Sold/Retired table afterward.
- [x] **Fixed 2026-09-25: Job Type options didn't match Simpro's own list** — our New Loan form only ever offered Demo/PMV/Repair, but Simpro's actual custom field (used for the same job) has Demo/PMV - On site/PMV - Back to base/Repair/Install-EquipChk/AED — sending bare "PMV" never matched either of Simpro's two PMV variants, so it silently failed to set correctly on the Simpro side. Both the desktop New Loan form (`#b-job-type`) and mobile's Book wizard sheet now offer the same 6 options Simpro does. Also, since Demo Type only means anything for a Demo job, it's now hidden and non-required on both platforms whenever Job Type isn't "Demo" (previously always mandatory regardless of job type). Needs testing: create a loan with each Job Type on both platforms, confirm Demo Type only shows/is required for "Demo," and confirm the job's Type field shows correctly in Simpro afterward.
- [x] **Fixed 2026-09-25: returning items could log a duplicate "Returned" history row with the customer/location blank** — reported as pairs of items each showing up twice in History at the same timestamp, one row with the real On Loan To/Location and one blank. Root cause: closing the Simpro job + pruning/deleting the loanDoc after a batch return can take a few seconds, and `confirmBatchReturn()`'s "Confirm Returns" button stayed enabled with the modal still open the whole time — an impatient second click re-ran the entire function on the same still-ticked items before the first run finished, and the second run's `logHistory()` read On Loan To/Location *after* the first run had already blanked them. Added a re-entrancy guard plus disabling the button for the duration (same pattern `submitBatchLoan()` already uses); single-item Return/Count (`submitReturn()`/`submitCount()`) were already safe since they null out `currentItem` synchronously before any of the slow async work. Mobile's equivalent already had a blocking loader covering this, but the guard now lives in the shared function itself. Needs testing: batch-return 2+ items, click Confirm Returns twice quickly, confirm only one History row per item.
- [x] **Fixed 2026-09-25: returning the item an overdue ("stuck") upcoming booking was waiting on didn't unstick it until the next page load** — asked while investigating the above: the Return itself always worked correctly (item genuinely marked returned), but nothing re-checked whether that freed up any stuck booking waiting on it — `activateUpcomingLoans()` (the sweep that converts a due, no-longer-blocked booking into a real active loan) only ever ran once, 500ms after `loadData()`. So the stuck booking kept showing its old blocked state right after the return, easy to mistake for the return itself having silently failed. `submitReturn()`, `submitCount()`, and `confirmBatchReturn()` now call `activateUpcomingLoans()` right after freeing the item, reusing the same single-loan activation helper (`_tryActivateUpcomingLoan()`) the reassign-edit flow already calls for this exact reason — a no-op when nothing is actually due/blocked. Needs testing: return an item that's blocking an overdue upcoming booking, confirm that booking activates immediately without needing a page reload.
- [x] **Fixed 2026-09-25: desktop header text wrapped ugly ("scrunched") on a narrower/laptop window** — reported via two screenshots: looked fine on a wide monitor, but on a narrower window "Scan / Loan" and "Live — auto-saving" each wrapped their own label onto 2 lines. Root cause: `.nav-tab` and `.sync-pill` had no `white-space:nowrap`, so once there wasn't room for everything on one line, the labels wrapped mid-phrase instead of the header adapting some other way. Fixed by making the nav tabs the one flexible element in the header — logo, version, sync status, Add Asset/Lock, and the user avatar all now stay full-size and never wrap or clip (`white-space:nowrap` + `flex-shrink:0`), while `.nav-tabs` gets `overflow-x:auto` and becomes a horizontally scrollable strip (thin scrollbar) once it runs out of room. Scales continuously with the actual window width rather than snapping at a tuned breakpoint, all the way down to the existing 700px cutover into the separate mobile layout. **Not visually tested in an actual browser** — no browser automation tool available this session; verified via careful review of the flexbox/overflow behavior instead of a live resize. Worth a quick manual check on a real narrow window.
- [x] **Fixed 2026-09-25: a stuck booking whose own end date had also passed would get silently reactivated instead of staying returned** — found by asking "what if the second (stuck) loan already finished by the time the item's returned?" right after the fix above. `_tryActivateUpcomingLoan()` only ever checked whether the item was still checked out elsewhere — once returned (no longer blocked), it would happily flip the item straight back to `OnLoanTo`/`Returned:'No'` on the stuck booking, with that booking's own end date already in the past — instantly overdue, instead of just staying returned. This was already reachable via the once-per-page-load sweep, but calling `activateUpcomingLoans()` right after every return (the fix directly above) made it much more likely to actually fire. A booking whose `endDate` is already before today is now treated as still-blocked (never activated), so it keeps surfacing via the stuck-loan reminder for an admin to extend its dates or cancel it — same handling as any other item it can't claim. Known remaining rough edge, not addressed: tapping "reassign" from the stuck-loan reminder for a booking stuck **only** for this reason (item is free, but the booking itself expired) shows "could not find what's blocking it," since that flow was built around the item still being on another active loan — the actual fix in that case is to cancel the stale booking via Delete, which the toast doesn't point at. Not needed unless this combination comes up in practice.

### Needs testing

- [ ] New "Build Kit" flow — item detail's "📦 Build Kit" button, Database tab multi-select as accessories, "Save as Kit Group" in the selection bar.
- [ ] Mandatory Shipping Island (North/South) field on the desktop batch loan form — same feature as mobile above, confirm required + saves correctly.
- [ ] Same-day loans (end date = start date) now accepted on the desktop batch form too.
